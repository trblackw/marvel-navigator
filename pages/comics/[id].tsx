import React from "react";
import { useRouter } from "next/router";
import { NextPage, NextPageContext } from "next";
import fetch from "isomorphic-unfetch";

const Comic: NextPage<{ id: string }> = () => {
  return <div></div>;
};

export default Comic;
